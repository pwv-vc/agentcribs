# brand

- Install @tailwindcss/typography for prose/content styling. Confidence: 0.70
- Use "waitlist" terminology (e.g., "apply to join the waitlist") instead of "receive consideration for upcoming events" or similar passive phrasing for the application flow. Confidence: 0.75
- Format/status badge colors should align with the brand's sage/green palette — avoid warm tones like amber or orange that clash with the overall brand aesthetic. Confidence: 0.75
- Use structured `--color-pwv-{name}` CSS custom properties for brand colors, with base colors (black, white, gray, green, teal) and soft/light variants using consistent prefixes (soft-green, soft-teal, soft-yellow, soft-coral, soft-lavender, soft-periwinkle, light-green, light-teal, light-yellow, light-coral, light-lavender, light-periwinkle). Confidence: 0.75
- Reserve `text-accent` for semver version flourishes only; headings, links, and captions should use pwv green brand text styles instead. Confidence: 0.70
- Use pwv brand colors (teal, lavender, periwinkle) for the semver diagram — not the green accent color. Version labels (0.0.1, 0.1.0, 1.0.0) should use pwv teal, not pwv green. Confidence: 0.80
- Ensure brand colors (especially lighter hues like lavender and periwinkle) have sufficient contrast against light mode backgrounds — if a color is invisible in light mode, use a darker variant or higher opacity for that mode. Confidence: 0.80
- For SVG elements that need per-element brand colors via CSS, render real `<path>` elements directly with the color class on the path (and set `stroke` directly in CSS) rather than using `<use>` + `currentColor` indirection through `<defs>` — browser behavior is inconsistent for `currentColor` resolving through `<use>` referenced paths. Use `<defs>` only for paths that need to be referenced by `<mpath>` in animations. Confidence: 0.75
- Move away from dark green sage for brand and taste contexts; shift the accent role (links, focus, status-accepted, all-active) to the teal family (`#0a6b63` deep teal in light, `#00d2c8` brand teal in dark) rather than just lighter greens. The cream background and black text stay as-is. Keep `--color-brand-green` (vivid `#00d22e`) for CTA buttons only as a Statement element. Confidence: 0.80
- Don't use `text-pwv-green hover:text-pwv-soft-green` on inline content links — the soft-green hover washes out and loses brand identity. Use the teal accent family (`text-accent hover:text-accent-hover`) for content links; reserve `pwv-green` for filled CTA buttons only. Confidence: 0.75
- For CTA button hover states and nav logotype hover, use a slightly darker PWV green (hue 145) rather than the teal/blue-green (hue 192) accent color — the teal hover reads as "off brand" on these elements. Reserve teal accent for inline content links and semver flourishes only. Confidence: 0.75
- Keep CTA button hover green at a similar lightness to `pwv-green` (oklch ~78% lightness) — the dark green hover (oklch 48% lightness) is too dark compared to the base `pwv-green`. Use a moderately darker green (oklch ~65-68% lightness) instead. Confidence: 0.72
- Use white text (`text-accent-text` or white) on hover for links that use brand green hover states, instead of retaining black text on the darker green background. Confidence: 0.72
- Prefers ultra-short, punchy staccato CTA copy for subscribe prompts (chose "Watch. Listen. Subscribe." over longer sentence-style options) — the shortest, most imperative variant wins for calls to action. Confidence: 0.65
- Podcast host cutout PNGs (which have real alpha transparency) are designed to sit on the chartreuse `#D5EF8A` backdrop — the same green as the podcast logo (`public/podcast/logo.svg`) — not on the card's dark `bg-bg-deep`. When rendering these transparent host images, back the element with that brand color so transparent margins don't let the underlying surface show through. Confidence: 0.7
