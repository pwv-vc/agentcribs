# icons
- Store all icon components in `src/app/components/icons/` with individual component files and a barrel export `index.ts`. Confidence: 0.70
- For event format badges: use a house-heart icon (lucide-style) for "in person" and a web camera icon for "remote" format indicators. Confidence: 0.75
- Use recognizable platform/brand icons alongside external links (Spotify, Apple Podcasts, YouTube, LinkedIn, X/socials) so link destinations are visually identifiable. Confidence: 0.75
- Icon components accept `{ className?: string }` and use `fill="currentColor"` (brand/platform glyphs) or lucide-style `stroke="currentColor"` with `strokeWidth={2}` (generic UI icons like globe), on a 24×24 viewBox. Confidence: 0.78
- When the user requests a nav/UI icon change, they paste the exact raw lucide SVG markup (from lucide.dev, `class="lucide lucide-...-icon"`) to specify the precise glyph — implement those exact paths as a project icon component and swap them in, rather than substituting or proposing alternate icon choices. Confidence: 0.7
- Header/nav icons should evoke their destination's content semantically (e.g., ticket-check for Events, mic-audio-lines for Podcasts) rather than generic placeholders. Confidence: 0.5
- Present external platform/social links as icon + label pill buttons (rounded-full, bordered, small text), with `target="_blank" rel="noopener noreferrer"` — applies both to collections of links and to single links; when a section shows a plain caption for something that already has a link in content frontmatter (e.g., a trailer's YouTube URL), replace the caption with the icon pill. Confidence: 0.80
- Once a lucide icon is established as the glyph for a concept (e.g., mic-audio-lines for podcasts), treat it as canonical and use it everywhere that concept appears — nav links and branding pills alike (e.g., swap it into the "Sponsored by AgentCribs" pills in place of the radio/check glyph), applying the change to all instances across pages rather than just one spot. Confidence: 0.65
